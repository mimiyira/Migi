<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Crypto Migi - Join the Future of Crypto Trading</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
    }

    body, html {
      height: 100%;
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      padding: 20px;
    }

    .container {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      max-width: 450px;
      width: 100%;
      padding: 40px 30px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      text-align: center;
      backdrop-filter: blur(10px);
    }

    .logo {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 15px;
      letter-spacing: 3px;
      color: #fff;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 15px;
      font-weight: 700;
      color: #fff;
    }

    p {
      font-size: 1rem;
      margin-bottom: 30px;
      color: #e0e0e0;
      line-height: 1.4;
    }

    form {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
    }

    input[type="email"] {
      flex-grow: 1;
      padding: 12px 15px;
      border-radius: 50px;
      border: none;
      font-size: 1rem;
      outline: none;
      transition: box-shadow 0.3s ease;
      min-width: 220px;
    }

    input[type="email"]:focus {
      box-shadow: 0 0 8px 2px #fff;
    }

    button {
      background: #ff6f61;
      border: none;
      color: white;
      padding: 12px 25px;
      border-radius: 50px;
      font-size: 1rem;
      font-weight: 700;
      cursor: pointer;
      transition: background 0.3s ease;
      min-width: 120px;
    }

    button:hover {
      background: #ff3b2f;
    }

    @media (max-width: 480px) {
      form {
        flex-direction: column;
      }
      input[type="email"], button {
        width: 100%;
        min-width: unset;
      }
    }
  </style>
</head>
<body>
  <div class="container" role="main">
    <div class="logo">Crypto Migi</div>
    <h1>Stay Ahead in Crypto Trading</h1>
    <p>Join our community of smart traders and get exclusive insights, tips, and early access to new tools.</p>
    <form action="#" method="POST" aria-label="Subscribe to Crypto Migi newsletter">
      <input type="email" name="email" placeholder="Enter your email" aria-required="true" required />
      <button type="submit">Subscribe</button>
    </form>
  </div>
</body>
</html>
